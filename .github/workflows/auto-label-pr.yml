name: "Pull Request Labeler"
on:
- pull_request_target

# this is an unnecessary comment but it lets me commit stuff
jobs:
  labeler:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v5
